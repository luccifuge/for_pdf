import tabula

def vse_tablisi(pdf):
  tables = tabula.read_pdf(pdf, pages='all', multiple_tables=True)

  for i, table in enumerate(tables):
    table.to_csv(f'test_{i+1}.csv', index=False)


pdf = 'FS IFRS Rus 0624 disclosed.pdf'
vse_tablisi(pdf)
